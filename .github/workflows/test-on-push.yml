name: Test on push
on: [push]
jobs:
  test-rspec:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - run: bundle install
      - run: rails parallel:setup
      - run: parallel_rspec

  test-cucumber:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - run: bundle install
      - run: rails parallel:setup
      - run: yarn
      - run: parallel_cucumber
